# WORK ORDER — PLACEHOLDER

<!-- ESCALATE-PLACEHOLDER: the real CHRONICLE-WORK-ORDER-ERA0-ERA1-CLUSTERS.md
     has not been committed yet. run_cycle preflight refuses to run while this
     marker is present. Replace this file's entire contents with the owner's
     work-order document (keep the filename work-order.md). -->

The pipeline cannot run without the owner's work order: it defines the phase
order, per-chapter model routing (§9.4), and the flag lists that separate
bounded patches from escalations.
